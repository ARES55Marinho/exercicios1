#include <stdio.h>

int main() {
    int contador;
    int soma = 0;

    for (contador = 1; contador <= 10; contador++) {
        soma = soma + contador;
    }

    printf("A soma dos numeros de 1 a 10 e: %d\n", soma);

    return 0;
}
